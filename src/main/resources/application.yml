spring:
  application:
    name: waver
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterAccess=5m
  datasource:
    url: jdbc:mysql://localhost:3306/bucket
    username: root
    password: 12345
  jackson:
    default-property-inclusion: non_null
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  web:
    resources:
      static-locations: classpath:/static/
  messages:
    encoding: UTF-8
    basename: messages
  cloud:
    gcp:
      project-id: "waver-478413"
      credentials:
        location: /home/opc/google-api-key.json
      pubsub:
        subscription-id: "play-rtdn-topic-sub"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui
    url: /v3/api-docs
    disable-swagger-default-url: true

waver:
  secret:
    name: USER_ID
    key: "wH5pbJDmpvIKWy3k5CXHTIApJEb3M/EAT7EwSrBscJA="
    access-token-validity: 300d
  file:
    upload:
      # 로컬 경로에 맞게 수정 필요
      path: upload
    images:
      path: /waver/image
    statics:
      path: http://localhost:8080/images
  service-key-path: /home/opc/waver-service-account.json

google:
  play:
    credentials-path: /home/opc/google-api-key.json
    package-name: "com.zinc.waver"

logging:
  level:
    root: info

